var max_busqueda = 10

function f_buscar_primos(max_busqueda) {
    for (let index = 0; index <= max_busqueda; index++) { // index de 0 a 100

    }
}

f_buscar_primos(max_busqueda);


let primos = [];

console.log(primos);

primos.push(50);
console.log(primos);
